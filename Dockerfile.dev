FROM python:3.6.8-slim-stretch

ENV PYTHONDONTWRITEBYTECODE='1'

COPY . /app

WORKDIR /app

RUN apt update -y && apt install make -y && sh -c "pip install -r requirements_dev.txt" && apt clean -y

CMD ["sh", "-c", "/usr/local/bin/gunicorn -c gunicorn.py feira.wsgi"]

EXPOSE 8000